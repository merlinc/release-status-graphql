// const fs = require('fs');

// const commit = JSON.parse(fs.readFileSync('../services/github/commit.json'));

// const resolvers = require('./resolvers');

describe('Builds / resolvers', () => {
  describe('mapGithubCommit', () => {
    it('works', () => {
      //       const data = resolvers.mapGithubCommit(commit);
      //       console.log(JSON.stringify(data));
      //       expect(data).toEqual({
      //         sha: '32911e08837cb71138d7cb1faef962ebff2b9629',
      //         // commits: [
      //         //     {
      //         //         sha: 'e87db5a6ed3bf0e753b82c5424947d1ef9e8f99b',
      //         message:
      //           'Feat/video schema org (#3040)\n\n* render schema.org\r\n\r\n* render schema.org script and test'
      //         //     }
      //         // ]
      //       });
    });
  });
});
